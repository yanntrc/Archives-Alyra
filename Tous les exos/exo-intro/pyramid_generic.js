let bloc = 'V'

console.log(bloc + '              ' + bloc)
console.log(bloc + bloc + '            ' + bloc + bloc)
console.log(bloc + bloc + bloc + '          ' + bloc + bloc + bloc)
console.log(bloc + bloc + bloc + bloc + '        ' + bloc + bloc + bloc + bloc)
console.log(bloc + bloc + bloc + bloc + bloc + '      ' + bloc + bloc + bloc + bloc + bloc)
console.log(bloc + bloc + bloc + bloc + bloc + bloc + '    ' + bloc + bloc + bloc + bloc + bloc + bloc)
console.log(bloc + bloc + bloc + bloc + bloc + bloc + bloc + '  ' + bloc + bloc + bloc + bloc + bloc + bloc + bloc)
console.log(bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc + bloc)